export enum ListGreConfig {
  App = 'app',
  Swagger = 'swagger',
  DataBase = 'database',
}

export enum EnvKey {
  AppName = 'APP_NAME',
  EnableSwagger = 'SWAGGER_ENABLE',
  DBHost = 'DB_HOST',
  DBPort = 'DB_PORT',
  DBUserName = 'DB_USERNAME',
  DBPassWord = 'DB_PASSWORD',
  DBDataBase = 'DB_DATABASE',
  DBLogging = 'DB_LOGGING',
}

export enum Security {
  auth = 'AUTH',
}
